### Post install

Once installation is complete there should be one super admin user which credentials are :

- the email address of the admin user who installed the app
- the admin password which was choosed during the installation steps

Once logged in you can then create new users for your app.
